Cross-cutting keyboard-accessibility and overlay-behavior sweep across the whole zellous app: Tab order through the whole shell, Escape-to-close on every overlay type (settings popover, emoji picker, command palette, context menus, video lightbox, auth modal, voice settings modal, drawer), focus-trap correctness inside each open overlay, focus-restore to trigger element on close, visible focus rings (:focus-visible) on every interactive element, color contrast in dark(ink)/light themes, aria-label/aria-modal/role on zellous-owned custom DOM (not SDK components). Also check for outline:none/outline:0 in zellous.css not paired with :focus-visible replacement. Verify prior documented fixes (focus-race fix, dialog semantics on Voice Settings modal, keyboard equivalent for drag-reorder) still hold. Other concurrent sessions own auth/settings, chat/messaging, servers/channels/moderation, voice, pages/forum/marketing surfaces - do not touch those or duplicate PRD rows.